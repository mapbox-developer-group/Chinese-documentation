/*---
title: 显示具有自定义属性的HTML聚类
description: 这一高级示例使用了带有HTML标记和自定义属性表达式的Mapbox GL JS聚类。如要在聚类中使用HTML或SVG代替Mapbox GL层，用户必须手动将聚类源与标记对象池同步，这样可以让标记对象池在地图视图变化时保持持续更新。
tags:
  - layers
pathname: /mapbox-gl-js/example/cluster-html/
---*/
import Example from '../../components/example';
import html from './cluster-html.html';
export default Example(html);
